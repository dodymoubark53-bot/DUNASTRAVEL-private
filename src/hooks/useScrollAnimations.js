// Deprecated in favor of native Framer Motion and CSS transitions.
// Preserved as no-op to maintain backwards compatibility.
const useScrollAnimations = () => {
  // No-op
};

export default useScrollAnimations;
